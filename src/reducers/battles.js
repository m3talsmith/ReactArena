const defaultState = {
    battles: []
}

export default function battleReducer(currentState = defaultState, action) {
    switch (action.type) {
        default:
            return currentState
    }
}