const START_BATTLE = "START_BATTLE"
const VIEW_BATTLE = " VIEW_BATTLE"
const JOIN_BATTLE = "JOIN_BATTLE"

export function startBattle(character) { }
export function viewBattle(id) { }
export function joinBattle(id, character) { }